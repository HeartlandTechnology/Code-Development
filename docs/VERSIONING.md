# Repository Versioning

Current version: 3.23.1

Version tracks the behavioral and tooling contract — **not the content**. Adding a paragraph to a
guide is not a version change; changing what the harness enforces, what a route resolves to, or
what a gate requires always is.

## The same-commit rule

A contract, route, MCP role, CI/security policy, verification requirement, canonical structure, or
repository operating-policy change updates **MODEL.md, VERSION, README.md, ABOUT.md, the affected
docs/patterns/adapters, and atlas.yaml together, in one commit.**

This is enforced, not requested. `atlas.py check` reads `VERSION` and fails when the ONE line
each site declares for it (`atlas.yaml/version_sites`, a pattern per file) disagrees, or when
`atlas.yaml`'s `version` does. **One declaration, read at a named line** — until 2.29.0 the rule
asked only whether the string appeared anywhere in a file, and a generated stamp satisfied it while
the README's navigation line named a release two versions old.

## What earns which bump

| bump | earns it |
|---|---|
| **major** | a route, manifest or contract shape that existing consumers must change to satisfy. Worked case: 2.0.0 renamed `provenance.authored` to `provenance.since` and bumped the manifest's own `schema` to 2 — a reader pinned to format 1 is told, rather than left to find a field missing. |
| **minor** | a new enforced check, a new instrument, a new invariant, a new route, or a gate that now blocks something it did not |
| **patch** | a correction inside the existing contract — a repaired check, a fixed parser, a stale claim re-measured |

**A new instrument is a minor bump even if no rule changed**, because the next reader's options
changed. Skipping the bump is how a tool ends up in the tree that nothing announces.

## Release procedure

The changelog line and the release are **two different artifacts, and for a long time only the
first existed** — 17 versions were named here and never tagged, so no reader could fetch the tree
any of them described. A version with no tag is a claim with no artifact.

1. `python scripts/atlas.py check` → must exit 0.
2. `python scripts/atlas_test.py` → must report all cases pass.
3. `ruff check .` → must exit 0.
4. Bump the six places above in one commit.
5. Add exactly one line here: `<version> <what changed, in one sentence>`.
6. `git tag -a v<version> -m "<the same sentence>"` on that commit.
7. Push the tag, then cut the GitHub release from it.

**Entries are kept in ascending semver order.** They were not: the list ran
`1.0.1 → 1.1.0 → 1.0.2 → 1.0.0`, which made three versions unreadable in sequence and is the
reason this section exists.

## One line per version, and it is the only changelog

No `CHANGELOG.md`, no release-notes generator, no conventional-commit tooling. A second changelog
is a second declaration: one copy goes stale and the next reader cannot tell which is live. The
commit message carries the detail; this file carries the sentence.

0.7.0 language-by-language MCP and VS Code capability routing
0.7.1 current-source licensing correction for Serena
0.7.2 Atlas CI runtime simplification
0.7.3 complete language targets and documentation reachability
0.8.0 repository wiki, code-specific routing, taxonomy, and branch/worktree policy
0.9.0 production operations matrix, assurance stack, GitHub security/AI finalization, and tool orchestration
0.9.1 validator repair and repository ownership/security metadata
0.9.2 Markdown-link parser self-test and escaping correction
0.9.3 link diagnostics and CI repository-surface checks
0.9.4 symlink-aware Markdown link validation
0.9.5 model/runtime routing, progressive context policy, language operating cards, and GitHub backend control plane
0.9.6 machine-routed language tool manifests, explicit task verification gates, severity/baseline policy, and dynamic Atlas planning
0.9.7 YAML-parsed harness, single-sourced task profiles, directory routing, manifest schema validation, generated language index, label check, CodeQL as default setup
1.0.0 first stable control contract: a tool manifest for every route, every atlas.yaml restatement generated and drift-checked, the learn command, provenance on manifests, every resolved count printed
1.0.1 mutation tests for the harness, route edge cases, working-directory-independent entry point, devcontainer
1.0.2 every hard invariant is enforced by a check or declared with its reason; the contract fails on an unowned invariant
1.1.0 all 25 hard invariants enforced by real checks over this repository's own artifacts (none declared), one planted defect per check, worktree lifecycle rules
1.2.0 contract instruments named with their blind spots, packprobe toolchain coverage, pyproject declaring the tools already in use, five classical laws harvested, GitHub audit re-measured and dated
1.2.1 private vulnerability reporting enabled and SECURITY.md re-measured against it; first tagged release verified on the remote
1.3.0 tool manifests validated against a JSON Schema that declares each entry's kind; the harness split into router, generators and contract; machine-readable route/plan/probe output and a generated llms.txt; every instrument and its closer declared in atlas.yaml; ghaudit compares the live GitHub controls to config/github-controls.json; main-protection requires the Contract and Dependency Review checks; MIT licence
2.0.0 manifest format 2: provenance carries the contract version it was authored at instead of a calendar date, and the contract refuses a date anywhere in a tracked file — this is a MAJOR bump because an existing tools.yaml reader must change; adds `atlas.py doctor`, the banner is content-addressed, and the research file is reviewed rather than harvested
2.1.0 four routes that fill a declared gap (OCaml, Scala, Swift, R) with the verdict recorded on all eleven candidates reviewed; the quantum domain made explicit with its own task profile and quantum_change gate rather than more language packs; the JavaScript family routed to the TypeScript pack; HTML href and src links checked, which the Markdown checker had never seen; every action pinned to a commit SHA; per-check OpenSSF Scorecard floors declared as a ratchet and compared by ghaudit, which now audits settings, webhooks, environments, releases and the score; docs/CERTIFICATION.md maps every check to its evidence
2.2.0 every example under examples/ made runnable and self-verifying across eight languages, exercised by the new exrun instrument in CI and routed by the atlas's own router; two were skeletons that could not run at all; a seeded property sweep over the router and the entry grammar, which found two inputs where entry_kind and entry_binaries disagreed; the build order declared in atlas.yaml with the gate that judges each step; hash-pinned dependency lock and licence gatekeeping on the required Dependency Review; every action pinned to a commit SHA; the administrator bypass removed from main-protection; dynamic badges and a badge-by-badge account of what each one proves
2.3.0 astshape refuses duplicate AST structures and code blobs by canonical hash rather than by text, with its caps declared as a ratchet that immediately fired on atlas.py check() and split it; the Forth route, which is a different execution model rather than another syntax, with the verdict recorded on APL, J, K/Q and eLua; a duplicate artifact_routes extension now fails the contract after '.fs' silently moved every F# file to the Forth pack; the ruleset PUT body is generated from the declaration, because a hand-written partial payload reverted two branch-protection settings with a 200 and no diff; the manifest skeleton generator emitted schema 1 against a schema that requires 2
2.3.1 Swift excluded from language detection with the trigger to reverse it: making the examples detectable expanded CodeQL to a language it cannot autobuild, and a scanner that fails every run gets ignored
2.4.0 a YAML loader that refuses a duplicate key anywhere, so a collision cannot resolve silently in any mapping; every tracked source file must parse, checked first, after a mechanical edit twice wrote a harness file that did not compile while every count still printed; VERIFY.md rebuilt around generated gates instead of tool names that disagreed with the manifests; CERTIFICATION's floors generated from the declaration and its own typed scores removed; SECURITY states what an outsider can and cannot do; two duplicating documents deleted and the two GitHub pages merged into one
2.5.0 two Scorecard arms moved by engineering rather than by prose: examples/go became a real module with FuzzPool, a fuzz target that asserts the four properties the bounded pool exists for (measured locally at 1.88 million executions, 15 new coverage inputs), fuzzed for a bounded 30 seconds on every pull request; and a release workflow builds one deterministic tarball of the routing surface, attests its provenance as a signed in-toto bundle and attaches artifact, digest and bundle to the release. Dependabot now watches every manifest that exists rather than one of three, a duplicate (ecosystem, directory) pair fails the contract because that collision hides in a list, and every claim asserting a present state was replaced by the version it was measured at or the instrument that answers it
2.6.0 coverage-guided fuzzing of the entry grammar and the router through ClusterFuzzLite, with the same target running as a fixed corpus wherever atheris is absent; five cross-reference assertions that check every roster in BOTH directions — a label with no route, a build-order step naming an undeclared gate, two instruments claiming one file, a runner for a route that does not exist, a self-composing tool profile; the OpenSSF Best Practices answer sheet declared as data and rendered with every evidence path as a link the contract validates; each badge now names the instrument that settles it instead of how it could mislead; the MCP example pinned off @latest, .editorconfig extended from four languages to the routes it has, and .gitignore taught about the release artifact and the fuzzing build
2.6.1 the fuzzing base image pinned by digest, after the per-check ratchet caught Pinned-Dependencies falling from 10 to 7 in the same scan that took the aggregate to 7.3
2.7.0 every pack declares how its language MEASURES a performance change, which is a different question from how it profiles one — optional in the schema so a reader pinned to format 2 is unaffected, present in all 35 packs so an absent role and an empty one are never confused, with every unconfirmed entry added to that pack's provenance.verify; the last unpinned dependency in the tree removed by deleting the writer rather than pinning it; and the uptime document gained the supervisor, wake, probe, drain and self-diagnosis doctrine — including why a liveness probe that calls a dependency adopts that dependency's outage, and why pinging your own service to keep it warm hides the cost that would justify fixing it
2.7.1 Scorecard floors raised to the achieved measurement — eleven checks at 10 and the aggregate floor at 7.5 — so the ratchet follows a rise as strictly as it refuses a fall
2.7.2 the last four stale sections removed — a topic roster left dangling under the sentence saying the page states none, version examples that age, a fourth verification ladder beside three others, and a per-language operations matrix that restated the manifests while omitting five routes and naming an SDK with no route; the Best Practices registry re-measured after a report that it was down, with the redirect chain that explains the report
2.7.3 both stuck dependency updates resolved by hand: the pyyaml floor raised to the version the lock already hash-pins, and the attestation action moved to v4.2.2 pinned by commit after its inputs and outputs were checked against that version's own action.yml — the bump Dependabot offered was a 2025 commit on the v3 line, older than the one v3 resolves to
3.7.0 one CLI, one harness, an MCP route: an install ships ONLY the launcher and runs the resolved atlas's own harness — the wheel used to carry eleven harness modules, a second copy pinned by pip, and `atlas check` and `atlas doctor` crashed in a real install while the install test ran only route, plan and process; that test now runs check, doctor, the roster and a dispatched instrument from outside the atlas. `thea` is the command (`atlas` still answers); `thea commands [--json]` lists every command and instrument from one parser and atlas.yaml/instruments, frozen as thea-commands/1; `thea <instrument>` runs enforce, staleness, branchstate and the rest by name; every command carries a help line or the contract fails. `thea-mcp` serves the same commands as MCP tools on stdio — schemas read off the parser, each call its own process with a timeout, and no write flag reachable. A front end is declared as a future reader of those records, not built. The instrument row keyed 'ruff check' pointed at the manifest validator; it is keyed by what runs.
3.8.0 a runtime keeps its own tools: Thea is added to an agent's layer and never swapped in for it. `atlas.yaml/native_agent_tools` declares, for every runtime in runtime_entry, where that runtime configures its tools and which of the CLI, the git hook and the read-only MCP route reach it; the new hard invariant `native_agent_tools_are_kept` (enforced by `nativetools.native_agent_tool_errors`) refuses a runtime with no declaration, an install that writes inside any runtime's tool configuration (the hook path is resolved from git, so core.hooksPath is honoured), a tracked tool configuration that denies or switches off a tool — by key, by a tools map set false, or by a denying value — read as TOML, JSON or JSON with comments, and an adapter with no 'Native tools stay' section. The planted suite now also proves `enforce install` writes nothing but the hook. GitHub topics declared to match the platform.
3.9.0 what actually passed: `verify.py` runs verification_policy/done_set — declared once, rendered into the entry files — and prints one verdict per gate from its EXIT CODE, with its time and the coverage line the gate states about itself; a gate not run (a mutating one under THEA_READ_ONLY, or a missing program) is NOT RUN and the run exits 2, never 0. `gate` printed commands that passed on nothing: the python check with no operand parsed zero files and exited 0, and the formatter gate was bare `ruff format`, which rewrites the tree. A per-file gate now carries its file (`takes_path`), and the formatter gate is each tool's check-only mode or ABSENT — the runnable count fell from 142 to 133, which is the honest figure. parser_discipline enforcers are read off the module AST, which found `atlascore.replace_once` naming a function that lives in safeedit. An audit that runs the planted suite beside a live editor is refused both ways: THEA_READ_ONLY refuses the suite, and scripted edits refuse while another process holds the suite lock.
3.9.1 entry pages say what Thea is, and verify names the cause: README, SECURITY, ABOUT and llms.txt rewritten so the first line reads "Thea Software, by Heartland Intel" and says what it does; the landing page shows a real `gate` answer as a generated block (never a transcript that ages), one section per idea, no count typed into prose, and the laws now agree with the rest — the exit code is the verdict. llms.txt no longer tells a reader to read MODEL.md before asking `gate`. The landing path's byte budget fell to what it now costs. verify blamed the first alarming line of a crashed suite (an expected print from a passing case); a traceback's last line is now reported as the cause, and a planted case holds it.
3.9.2 the MCP route never claims a protocol it does not speak: `initialize` echoed whatever revision a client named, including one not yet written. It now answers the client's revision only when it is declared in atlas.yaml/external_versions, and otherwise the declared specification; every tool carries readOnlyHint, destructiveHint and openWorldHint annotations for the client's UI, while the guarantee stays the construction (no write flag is reachable). The MCP conformance case asserts both.
3.10.0 one body for every runtime, and an install that keeps your hook: CLAUDE.md is now `@AGENTS.md` plus only what is Claude Code's, instead of a second generated copy of the same body; contextcost follows `@path` imports, so the start-up figure is what Claude Code actually reads, and the agent entry path stayed under its ratchet by removing duplicates rather than raising it. AGENTS.md says which file each runtime loads (Hermes: .agent/bootstrap.json). `enforce.py install` refused any repository that already had a pre-commit hook, Thea's own included; it now moves a foreign hook to pre-commit.legacy (never deletes it), runs it first with its veto intact, `uninstall` restores it, a tracked core.hooksPath is refused by name, and a pre-commit-framework repository is given its `repo: local` entry with nothing written. verify blamed a finding line when a suite's own FAIL line came later; the last FAIL line is now the cause. ci_enforces_contract is DERIVED from verification_policy/done_set instead of a typed list — which found at once that no workflow ran the linter; CI now runs it, pinned by SHA and version.
3.10.1 the MCP route answers the client that is actually connecting: Claude Code asks for a published revision no row named, so every handshake from it was answered with mcp_specification and refused, while each probe here passed because it asked with a revision already listed. The captured revision is declared under the client's name in external_versions, and the probe now asks with every declared revision and fails on any answered with the fallback — planted and caught. Ledger: a_check_proven_on_one_shape_of_input, second sighting.
3.11.0 every declaration has a reader: `declcheck.py` and the hard invariant `declarations_are_read` read the blocks an audit found nothing reading. issue_routes may use only routes, gates, profiles, runtimes, MCP servers or the closed `issue_route_terms` list, so a typo is refused instead of read as a topic; model_routes may name only runtimes and never a multi_agent_host (native_toolchain is now a declared runtime role); front_end/reads was already split on its commas into fragments and is now whole, and the split is refused; drift_review must be a valid split with paths in the tree; the paths, `atlas.py` verbs and flags inside topologies and first_sweep must exist; every landed state must be one branchstate reports. Each has a planted defect, and the clean tree sweeps to zero. The first draft of the verb check never fired — `scripts/atlas.py` is not the word `atlas.py` — and its planted case said so. Two invariant wrappers that were one function are one factory. verify now reports a guard's own verdict word (DUPLICATE, MUTATION DID NOT APPLY) as the cause when no FAIL line was printed.
3.12.0 Thea generates the sandbox it cannot enforce, and says what each setting gets: `sandboxgen.py` turns a task contract and config/agent-sandbox.json into the host rows the runner prints UNOBSERVED — `docker run` flags with no network, a read-only root, only the worktree writable, a non-root user with no capabilities, bounded cpu, memory and pids and the contract's wall clock as a hard timeout, or a macOS sandbox-exec profile proven on a Mac to allow a worktree write and refuse a home write and a connect; a network allowlist is refused, since a flag cannot be an egress proxy. The landing page renders first_sweep/settings as one table — chats with and without tools, agents, CI, a retrieval or RAG pipeline (the retrieval_change gates), an autonomous run (the sandbox), a memory — so 'who is this for' is answered by the declaration. The gate-class table moved to docs/VERIFY.md and repeated paragraphs left the landing page, which stayed under its byte ratchet rather than raising it.
3.13.0 a killed run cannot leave a plant behind, and the ledger is a command: an external reviewer's timeout killed the planted suite mid-case, the tree kept `runtime_dependencies: 4`, and the next verify reported the plant as the repository's own drift. Every plant is now journalled before it is written; `atlas.py check` refuses a leftover by name, a timeout's SIGTERM runs the restores, and `atlas_test.py --restore` reverts what a SIGKILL left — never a file edited since. `thea failures [<id>]` prints the failure ledger most-sighted first, and the new shape is in it. verify prints one cause once, and every later gate that tripped on it reads 'same cause as'. The landing page states the breadth up front with the headline figures generated beside it; the per-runtime start-up table moved to models/README.md so the page stayed under its ratchet.
3.14.0 measurables that hold, work that knows where it returns, and no dead symbol: `scoreboard.py` and the invariant measurables_only_rise hold every recorded benchmark's WORST model at a declared floor — below it the build fails, and above it by more than the slack it fails naming the new floor, so a gain is locked in by the run that earned it. `orphans.py` and no_orphaned_symbols refuse a top-level harness symbol named only by its own definition; its first sweep deleted one dead function and replaced an f-string dispatch no search could see with a named map. Every process now declares what it `returns`, and `process` prints it. `thea steps <path> --runtime <id>` gives the ordered plan for one runtime — how it reaches Thea, what to load, the commands that prove the change, the budget, the sandbox, the done check and the return point (a pull request for an agent, a labelled checklist and the report verb for a chat). A byte-budget or line-cap breach names the largest parts to cut. verify colours its verdicts in a terminal (never under NO_COLOR) and records output bytes and calls per gate. The MCP route serves each atlas.yaml section as a resource and two prompts, plan-change and review-diff — context paid when read, not on every request. Measured over sixty days, hand-written guides drift and generated files cannot, so the scripts and verbs every guide under integrations, patterns, systems, wiki and languages cites must exist.
3.15.0 every version releases itself, and every claim says how far it reaches: 3.8.0 through 3.14.0 merged green and none was released, because tagging lived in `branchstate --sync` and an auto-merged lane never runs it — the contract badge still read v3.7.0. A push to main that changes VERSION now creates the tag inside the release workflow, which then checks, builds, attests and publishes; the missing tags were backfilled. `check --json` emits every finding with its declared severity, recorded by check() itself rather than parsed from what it prints; `verify` is a command, and the MCP route runs every call under THEA_READ_ONLY, so its verify reports the planting suite NOT RUN and can never write. The landing page and docs/CERTIFICATION.md now state what the measurements do not prove — routing and bookkeeping inside the declared surface, authored by the system under test, parsing not behaviour, bounding only the agent that asks.
3.16.0 the commit hook proves behaviour where it is cheap, and every measurement says how independent it is: a staged file that is a test now runs under its own runner when that runner takes one file (gate_tools/unit_tests/per_file_runners), so a failing test is refused at commit, not only a file that does not parse. Every benchmark floor declares its `evidence` — self-authored questions with a held-out split, self-authored tasks, planted breaks — and scoreboard prints it beside the number; a floor with none is refused. The ledger gains the shape an agent kept repeating here: a ratchet breach worked as a writing exercise instead of read and built once.
3.17.0 an agent can edit through Thea without leaving the controls: `thea_edit.py <contract>` is a separate, opt-in MCP route with one tool, apply_edit. Every write passes the sandbox and budget verdicts BEFORE it lands, goes through an anchor that must match once and a write read back, and every write and refusal is sealed onto the task's audit chain; it holds no credential, runs no command, never starts under THEA_READ_ONLY, and landing stays with branchstate. Token cost is now held too: scoreboard ceilings keep the most expensive model's tokens per question at or under a limit that only falls. A plain-words search reaches the declared identifier that contains a term (type check reaches compiler_or_typechecker), derived from atlas.yaml, never a typed synonym list. `thea steps` carries the process's stop, escalate and acceptance conditions, so an agent knows when it is done and when to hand back.
3.18.0 the arm nobody ran, and the rule nothing enforced: `agentbench.py` runs a headless agent on real bug-fix tasks in a scratch copy — blind, and with Thea's gate answer — and scores the task's own test exit code, recording seconds, turns, tokens and dollars at the source. Its first run is honest and unflattering: both arms solved all three tasks, a ceiling effect; harder tasks are its closer. The public-repository rule is now a hard invariant, public_tree_leaks_nothing: `leaks.py` refuses a home path, a personal address, a private network address or an internal hostname outside declared placeholders, and asserts that logs, shell histories and traces stay gitignored — its first sweep found two false positives, fixed in the patterns before it shipped.
3.19.0 an agent can change roles and pick up where work stopped: atlas.yaml/agent_roles declares the roles a user may direct an agent into — implementer, debugger, reviewer, verifier, researcher, orchestrator — each with its task profile, what it may and may not do, what it hands back and when it ends, checked by declcheck; `thea role` prints one. `thea resume` rebuilds interrupted work from the tree, never from memory: the lane, uncommitted changes, a plant a killed run left, the last audit event and the last verify record (now kept in .git), and names one next action. AGENTS.md tells every runtime both exist, paid for by lowering the mistakes-list budget. The agent arm gained a task where Thea's gates matter: blind 3/4 gates clean against 4/4 with Thea — one run, a sample, held by a floor. Package metadata and CITATION.cff make the project findable by search, agents and citation tools.
3.20.0 a vague prompt becomes a task or a question, failures teach, and the loop is fast enough to use: `thea intake "<prompt>"` routes the files a prompt names, reads a role and change class from declared names only, and asks only what blocks action — no target, or no stated change — exiting 3 until it can act; a missing acceptance becomes the file's own gates, stated as an assumption, and two named change classes run the stricter union of their gates instead of stopping to ask. verify now counts each failing cause once per run in a bounded store in .git and prints RECURRING when a cause returns, and `thea resume` surfaces it — the second time is a rule, mechanically. `thea verify --changed` runs each changed file's own declared fast-loop gates plus the contract in seconds; its first run applied a formatter this harness never adopted to every file, so the fast loop now holds only verification_policy/fast_loop, and per-file lint honours the configured excludes.
3.21.0 no number drifts, and every decision stands on evidence: a number typed beside a counted thing in any tracked document fails the build unless it is stamped as a record (measured at vX) — the first sweep found one stale count (29 language routes against 36), three caps restated beside the constants that enforce them (an advisory function limit contradicted the enforced one), and fourteen historical measurements now stamped with the version their line was written at, read from git. Every system-design decision carries evidence — data with its primary source, the trend, and who pays for the wrong choice — for all of them, researched with each unfetched source labelled; the research corrected one record (erasure coding tolerates one more loss than triple replication at a quarter of its overhead, not similar tolerance at half). `thea decide` prints it. What the commit hook enforces, verify and CI must enforce too. `thea steps --tier small|mid|frontier` scales scaffolding to the model; measured on haiku it changed nothing on these tasks (2/2 both, +8 s), so mid stays the default and the lever is recorded as unproven.
3.22.0 the timed session is a declaration, and a percentage is a claim: `atlas.yaml/cadence` holds the phases, the work-in-progress limit of one, the checkpoints, the definition of done and the rule for a box that expires mid-change; `thea cadence [--minutes N]` prints it as a clock for any length and declcheck refuses weights that do not sum to the box, a missing or duplicated verification reserve, a done the agent judges for itself, or nothing named as never done under pressure. Its shape is researched, not invented — fixed phases over free agency (Agentless), a reserve taken at the START because premature commitment is the measured failure of coding agents, one change in flight, and most Scrum ceremonies dropped because they coordinate humans; the box LENGTH is a cost choice and says so. The number guard now reads percentages, which its first version could not: its escapes are the rule's own (name the instrument, stamp the version, attribute the quotation), a command line is not a source, and the sweep found a typed 90% on the Claude adapter — the claim moved to the generated block where it is measured. The adapter now says what to do whatever model is running: tiered steps, intake, role, resume, cadence, and a co-authorship line read from the session rather than copied.
3.23.0 a handoff carries a brief, and an answer is a hypothesis: the two largest measured failure categories across multi-agent systems are specification gaps and inter-agent misalignment (MAST, arXiv:2503.13657) — brief quality, not model quality — so `atlas.yaml/delegation_contract` declares the six a handoff must carry (goal, scope, acceptance, returns, forbidden, read_only), each with the reason it exists, and `thea delegate` prints them rather than leaving each caller to improvise. declcheck refuses a missing field, a field with no reason, and an on_return that does not say a returned result is a hypothesis until an instrument here confirms it; the subagent adapter now points at the same declaration instead of describing it separately. The number guard counts arXiv and DOI identifiers as the sources they are.
3.23.1 `--sync` can finally clear the lanes it landed: it decided a lane was finished with `git branch -d`, which asks about ANCESTRY — and a squash merge destroys ancestry, so every lane this repository lands read as "holds work not in the default branch" and was kept forever. Found by a finalization sweep against two lanes whose work merged a day earlier. It now compares PATCHES with `git cherry` when -d refuses: a lane holding at least one commit and no unmerged patch is deleted and says why. The planted case builds a real squash merge and asserts both halves — the lane reads finished AND `git branch -d` still refuses it, so the case cannot pass by ancestry alone.
3.6.1 a squash-merged lane reads FINISHED: staleness.py compared commit hashes with the base, and a squash merge lands the same content under a new hash, so v3.6.0's own lane still read "active, ahead 1" after it merged; the verdict now also asks whether the lane's tree equals the base
3.6.0 workflows measured: workflowbench.py scores what agents commit alone (with and without the enforce hook) and hand to each other (a planner's task contract, blind, schema-only and with Thea) — with the schema alone Haiku, Sonnet and Opus chose the right gates 0 of 6 times, with Thea 6 of 6; solo, 24 of 24 runs committed clean with or without the hook, and a planted broken commit is refused, so the scorer is proven sensitive; every subprocess call in the harness carries a timeout, after a hung model call crashed a benchmark and took its finished results with it; staleness.py names what was edited longest ago, which worktrees are finished or stale, and — on any STRUCTURAL change, triggered in the session rather than on a schedule — every file older than the drift horizon that still names an identifier the change removed; TypeScript checks per project with tsc
3.5.0 a landing refuses to push when a clean checkout of HEAD fails its gates: branchstate --land runs the contract and the mutation suite in a throwaway worktree first, with no flag to skip it — after a lane landed while its own clean-checkout run printed a failure, the verdict displayed and never obeyed
3.4.0 enforcement reaches the moment an agent commits in ANY repository: enforce.py install writes a pre-commit hook that runs each staged file's check-only command and refuses a failure, under every agent that commits through git; measured by planting a syntax break into every example whose toolchain is installed — refused all of them, with each untested file counted and a correct file refused (a misfire) failing the measurement; that measurement caught the check-only commands themselves misfiring on correct C++, Swift, Go, Gleam and TypeScript, and ocamlopt writing beside the source, all corrected and verified
3.3.1 YAML values are generated, not hand-quoted: `safeedit.py quote` emits the first style that reads back exactly as a plain value and inside a flow mapping, proven over hundreds of seeded hostile strings; the thea skill tells every agent to use it
3.3.0 `atlas gate <file>` with no gate prints the numbered commands that prove a change there — one verb to start, no vocabulary first; every entry file tells a reader who was handed the link to say what it can do NOW, by setting, instead of stopping at a summary (a chat given only the link had summarised and offered nothing); CHAT.md maps who hands what to whom — person, chat, agent, model, several agents or chats — to the artifact that crosses; the README says the repository was formerly named Code-Development, after a chat invented a second repository from old links; flow_split_errors refuses a YAML flow value split on a comma, which had silently truncated six declarations — now refused by the strict loader at parse time in every YAML file, and values are generated by `safeedit.py quote`, round-tripped over hundreds of hostile strings, instead of hand-quoted
3.2.0 the thea skill ships in the tree (skills/thea/SKILL.md), portable to any skill-aware runtime and readable by any model, and every entry file maps a new verb, report, to it — a chat answers with a ready-to-paste ledger entry; the README reads on a phone: capabilities, Start here and the repository facts are bullets, not wide tables; the ledger records a wait that matches its own command line, and a second sighting of a roster typed from memory; the intake rule forced its first graduation — the grep-shadow lesson became a standing verdict, since no in-tree guard reaches the harness shell; the skill and the README say when to run it
3.1.0 /thea feeds the failure ledger the moment a break is seen: a shape that cannot be guarded yet enters with `intake: <version>` and check refuses it two minor versions later unless it has become a guard or a standing verdict; first intake is a_search_that_skips_what_it_cannot_see (the agent shell's grep is an embedded ugrep that skips ignored files); the README tells an AI opening the link where to start
3.0.0 Thea Software by Heartland Intel: the account login moved to HeartlandIntel and the repository was renamed thea-software (the brand is Thea Software — a Thea AI already exists), applied from the identity declaration; the published interfaces moved with a compatibility path each — THEA_ROOT (CODE_DEVELOPMENT_ROOT still read), the thea-harness distribution, the printed `Thea Software contract`, the thea-software-wt worktree convention; the identity check now scans atlas.yaml outside its own block, after the chat install text it generates was found carrying the old owner; the licence names Heartland Intel by the owner's decision
2.30.0 taskbench.py measures what Thea improves beyond routing — naming a failure from its symptom, listing the checks a change needs, spotting a line the build refuses — blind against with-Thea per model, and refuses to record a non-answer or an arm where Thea scores below blind; every change class now extends source_change, after a model handed the policy table added the format, compile and unit gates an API change had been allowed to skip; failure modes carry a `tell` that separates each from its neighbours; examples run for nim, ocaml, elixir, gleam and zig from a GENERATED Brewfile, on a scheduled macOS workflow that the policy had named and nothing implemented; invisible Unicode and sizes typed into entry documents fail the build; docs/ENCODING.md judges each compression technique adopt, measure-first or refuse; the failure ledger gained the shapes this release was built on (a check satisfied by a rendering, a class that skipped its base, a non-answer scored as wrong, a named mechanism that does not exist, a bad result captioned and passed), and every backticked repository path in a document must exist; the gate review found 21 gates resolving to the plain test runner and gates resolving to a bare runtime or build tool that ran the program or printed help — race_detection now means `go test -race` where it exists and is absent with its closer elsewhere, compiler_or_typechecker names each toolchain's check-only command (bash -n, py_compile, node --check), and the invariant gates_resolve_distinctly fails any collision outside declared alias groups; zig comes from its own digest-verified release instead of a 1.75 GB Homebrew LLVM
2.29.0 Claude is measured first — Haiku, Sonnet and Opus through a `claude-cli` provider whose fixed preamble is calibrated out, re-run with `abtest.py --claude`; the README's measured section is plain bullets; a version site is read at its declared line, not anywhere in the file, and a typed `contract vX` that is not VERSION fails; `dependency_graph` and `vulnerability_scan` resolve to each package manager's built-in tree or audit command, or to a declared absence, where they had returned a bare tool name that ran no check; the invariant `dependency_count_is_the_closure` holds the advertised dependency count to the hash lock and to the installed metadata walked transitively; docs/DEPENDENCIES.md covers direct, transitive and micro dependencies and the wiring between modules
2.28.0 every stop and escalate condition names who decides it (9 by a function, 10 by a named closer) and an unowned one fails the contract; `atlas gate` answers with one command, measured as the most accurate and cheapest answer (98.2% at 10% of the tokens of reading every pack); waiting is on a condition through resilience.wait_until, and a bare sleep is refused; resilience.py retries only what can succeed and latches on a 402; safeedit.py makes scripted edits refuse a missing or repeated anchor and prove the YAML key set moved only as intended; releases are tagged by branchstate --sync after a merge, closing a nineteen-version gap in which no release was published; the A/B spans 8 models on 4 providers (98.1% scoped against 68.1% blind) and counts an empty answer apart from a wrong one, after a reasoning model's spent output cap read as 33%; seven policy invariants are property-tested over seeded inputs, each killing its mutant; CHAT.md installs the atlas into any chat that cannot run code
2.27.0 every (pack, gate) pair now RESOLVES — 315 of 315, 213 to a runnable command and 102 to a declared absence naming its closer, where 44 previously named a tool with nothing to drive it and resolved to silence; `gate_resolution` replaces a two-state answer that could not tell a closed ecosystem gap from an undeclared one, `role_coverage_errors` refuses the third state, and every driver added was a command the same manifest already declared. identity.py left the wheel: it reads `git ls-files`, which an install has no tree to answer, so it had shipped as an arm that could never run — the install footprint fell to 184 KiB and the ratchet with it. The A/B gained a third question kind over the newly answerable formatter gate, and the README's measured table now carries what a session PAYS rather than what it is handed
2.26.0 an agent runtime opening this repository is now given its instructions automatically: CLAUDE.md and AGENTS.md are GENERATED from one body alongside llms.txt, so three conventions are served by one declaration and the contract fails on drift — which also replaces the old refusal of a root AGENTS.md with something strictly stronger; docs/CONSUMING.md states how another repository, a vault or a runtime uses the atlas without vendoring it, including verifying the attested release artifact; the runtime roster in MODEL.md is generated from model_routes and runtime_roles; and the eight runtime adapters stopped being 13-line stubs — each now says how the atlas is loaded there, what the runtime is routed for, and the mistake that runtime makes
